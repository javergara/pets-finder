# Estado actual

**Fase activa:** backlog — `feature_list.json`
**Feature actual:** ninguna `in_progress`. `12-sponsorship` → **`done`** (revisor, sesión independiente, 2026-08-03). Ver "Cierre de `12-sponsorship`" más abajo para el detalle completo del veredicto.

## Cierre de `12-sponsorship` (revisor, sesión independiente, 2026-08-03)

**APROBADA.** Verificación de cero desde disco (sin confiar en las notas del implementador/líder), agente revisor fresco sin memoria de la sesión de implementación.

- `bash init.sh` corrido en esta sesión, en verde de punta a punta: **176 tests de API + 62 de frontend**, lint/formato limpios (`ruff`, `black`, oxlint, `tsc -b`, prettier).
- **Grep independiente de pasarela de pago** (restricción crítica, acceptance #7): `grep -rniE "stripe|pse|wompi|paypal|mercadopago|payu|epayco|checkout\." ` corrido por el propio revisor sobre `models/sponsorship.py`, `schemas/sponsorship.py`, `routers/sponsorships.py`, `Apadrinar.tsx` (y ampliado a `Apadrinar.test.tsx`, `MiPerfil.tsx`, `MiPerfil.test.tsx`, `api/client.ts`, `api/types.ts`, `tests/api/test_sponsorships.py`) → **cero coincidencias** en ambos casos (exit code 1). También sin `requests.`/`httpx.`/`urlopen`/`fetch(` salientes en el backend nuevo. `POST /api/sponsorships` es exclusivamente un `INSERT` en `sponsorships`, confirmado leyendo `routers/sponsorships.py` línea por línea.
- Acceptance #1-3 (endpoints `GET /api/pets/necesitan-apoyo`, `POST /api/sponsorships`, `GET /api/users/{id}/sponsorships`): código leído línea por línea, reutiliza `services/deck.py::es_dificil_de_ubicar` vía `_pet_out(pet, home=None)` de `routers/pets.py` (no reinventa el criterio, confirmado por import directo). `META_MENSUAL_COP = 200_000` documentada explícitamente en comentario de módulo como "meta ilustrativa fija de demo, sin origen en ningún campo real de Pet/Shelter (HANDOFF §5.7)". Cobertura de tests confirmada: `tests/api/test_sponsorships.py` (19 tests) cubre filtrado por cada criterio de `es_dificil_de_ubicar`, exclusión de no disponibles, `porcentaje_cubierto` con 0/1/varios apadrinamientos y tope 100%, exclusión de `activo=False`, 201/404×2/422×2 en `POST`, listado con activos+inactivos y `novedad` condicionada a `activo`, 404 de usuario inexistente.
- Acceptance #4 (wiring de stubs 07/09): diff completo de `routers/users.py`, `routers/shelters.py`, `schemas/user.py`, `schemas/shelter.py` leído — en ambos routers **solo** se tocó la línea de `apadrinamientos`/`apadrinamientos_recaudados_cop` (antes `0` fijo, ahora `COUNT`/`SUM` real de `Sponsorship`) y su comentario/docstring; ninguna otra métrica (`matches_activos`, `visitas_agendadas`, `mascotas_publicadas`, `interesados_este_mes`, `adopciones_cerradas`) fue tocada. Comentarios "siempre 0" confirmados removidos de `schemas/user.py`, `schemas/shelter.py` y el espejo `src/web/src/api/types.ts::ShelterMetrics`. Cubierto por `tests/api/test_sponsorships.py::test_apadrinamiento_activo_refleja_en_metricas_usuario`/`::test_apadrinamiento_activo_refleja_en_metricas_refugio` (con un segundo `Sponsorship` inactivo para probar que no se suma) y `tests/api/test_users.py::test_obtener_perfil_apadrinamientos_es_cero_sin_sponsorships` (caso base 0).
- Acceptance #5-6 (`Apadrinar.tsx`, sección "Mis apadrinamientos" en `MiPerfil.tsx`): código leído completo. 3 niveles ($30.000/$70.000 destacado con `border-ochre`/monto libre), selector mensual (default)/único, lista "Necesitan apoyo ahora" con barra de progreso `bg-forest` y `%`, botón Apadrinar por mascota. `MiPerfil.tsx` gana sección nueva con estado vacío + link a `/apadrinar`, o lista con monto/periodicidad/novedad e etiqueta "Inactivo" si corresponde. Cubierto por `Apadrinar.test.tsx` (5 tests) y `MiPerfil.test.tsx` (2 tests nuevos).
- **Gap encontrado y corregido por el revisor** (no delegado): `docs/architecture.md` §2 todavía decía *"`Sponsorship` se añade cuando se retome la feature de backlog `12`"* pese a que la tabla ya existe e implementa desde el Paso 1. Es un cambio de documentación (no código de producto ni tests), así que el propio revisor lo corrigió: la línea ahora lista `Sponsorship` (`models/sponsorship.py`, feature `12-sponsorship`) junto a las demás entidades, con la misma nota de "sin pasarela de pago real". `bash init.sh` re-corrido tras el cambio, sigue en verde.
- `changes.md` confirmado con 4 entradas fechadas 2026-08-03 referenciando explícitamente `12-sponsorship` (pasos 1-4).
- Confirmado programáticamente que ningún otro item de `feature_list.json` quedó `in_progress` tras el cierre.
- Recorrido manual en navegador real: **no repetido** (ya realizado en la sesión previa según instrucción explícita del orquestador — apadrinar $70.000 a Rocky, progreso 35% reflejado de inmediato, métricas de `/perfil` y `/refugio` actualizadas, sección "Mis apadrinamientos" con novedad, sin errores de consola).
- `data/app.db` reseteado con `python3 scripts/seed.py` al cierre de esta sesión de revisión.
- `feature_list.json`: `status` de `12-sponsorship` → `done` (edición quirúrgica, `acceptance` de 7 líneas sin tocar). Validado con `python3 scripts/validate_feature_list.py feature_list.json` (exit 0).

## Verificación del líder antes de planificar (2026-08-03, releído el código real, no solo el documento de diseño)

- `src/api/adopta_api/models/`: confirmado, **no existe** `sponsorship.py` ni clase `Sponsorship`. Archivos actuales: `base.py`, `chat.py`, `home_profile.py`, `match.py`, `pet.py`, `shelter.py`, `swipe.py`, `user.py`.
- `schemas/user.py::UserMetricsOut.apadrinamientos` (línea 49) sigue precedido del comentario exacto `# Siempre 0: no existe tabla Sponsorship todavía (feature 12-sponsorship, backlog).` — confirmado también el stub `apadrinamientos=0` hardcodeado en `routers/users.py` (dos apariciones: `registrar_usuario` y `obtener_perfil::obtener_perfil`).
- `schemas/shelter.py::ShelterMetricsOut.apadrinamientos_recaudados_cop` (línea 15) tiene el mismo comentario exacto. Espejo en frontend confirmado: `src/web/src/api/types.ts::ShelterMetrics.apadrinamientos_recaudados_cop` (línea 141) con el comentario `// Siempre 0 en el MVP: no existe tabla Sponsorship todavía (feature 12-sponsorship, backlog).` — hay que actualizar también este comentario del frontend en el Paso 2 (el documento de diseño lo menciona explícitamente en su §3).
- `services/deck.py::es_dificil_de_ubicar(pet: PetOut) -> bool` confirmada con esa firma exacta (línea 18): recibe un `PetOut` (schema Pydantic, no el modelo SQLAlchemy `Pet`), devuelve `True` si `edad_meses > 84` (constante `EDAD_MESES_SENIOR`), o `"necesita experiencia" in pet.tags`, o `dias_publicada > 90` (constante `DIAS_PUBLICADA_DIFICIL`) usando `pet.publicado_en`. **Nota para el implementador del Paso 2**: para reutilizarla en el nuevo endpoint hace falta construir un `PetOut` a partir de cada `Pet` de SQLAlchemy — el patrón ya existe en `routers/pets.py::_pet_out(pet, home)` (con `home=None` no calcula afinidad, que es justo lo que necesita esta lista pública sin `user_id`).
- `design/prototypes/HANDOFF.md` §11 ("Fuera del alcance de este prototipo") confirmado, cita textual: *"Pasarela de pagos real, verificación de identidad, contrato de adopción digital, transporte, seguimiento post-adopción, moderación y reportes, y la app nativa. Todos están previstos en el modelo pero no diseñados."* — confirma que `12-sponsorship` es, por diseño del propio prototipo, un registro de compromiso sin movimiento de dinero real. Esta es la restricción crítica que el revisor debe volver a confirmar por grep en el Paso 5 (cierre).
- `routers/chat.py` (feature `11-chat`, patrón de referencia para el nuevo `routers/sponsorships.py`) confirmado: agrupa lógica propia de la feature en un router nuevo aunque sirva paths bajo distintos prefijos (`/api/matches/...`), registrado en `main.py` junto a los otros 6 routers (`pets`, `swipes`, `matches`, `users`, `shelters`, `chat`).
- `models/__init__.py` y `models/match.py` confirmados como el patrón exacto a replicar para `models/sponsorship.py` (mismo estilo de `Mapped[...]`/`mapped_column`, `ForeignKey`, `default=lambda: datetime.now(timezone.utc)`).
- `schemas/shelter.py::DescartarIn` (línea 58) confirmado como el patrón de `field_validator` a replicar para validar `periodicidad` en `SponsorshipIn`.
- Estado base confirmado en verde antes de empezar (heredado del cierre de `11-chat`): `pytest tests/api -q` → **157 passed**; `npx vitest run` (frontend) → **55 passed**.
- Constantes de sesión frontend confirmadas: `src/web/src/lib/constants.ts::DEMO_USER_ID`/`DEMO_SHELTER_ID`, `src/web/src/lib/session.ts::getActiveUserId()` — mismo patrón a usar en `Apadrinar.tsx`/`MiPerfil.tsx`.
- `App.tsx` confirmado: `/perfil` y las rutas `/refugio/*` ya están fuera del bloque `RequiereHomeProfile` (líneas 48-61) — la nueva ruta `/apadrinar` va junto a `/perfil`, fuera del guard, mismo criterio (no hay razón funcional para exigir el cuestionario para donar).

## Restricción crítica de esta feature (repetir en cada paso, verificar explícitamente en el cierre)

**Cero integración de pasarela de pago real.** Ni Stripe, ni PSE, ni Wompi, ni ningún gateway externo, ni redirección a checkout de terceros. `POST /api/sponsorships` es exclusivamente un `INSERT` en la tabla `sponsorships` — un registro de compromiso, no una transacción de dinero real. Esto no es una decisión nueva de esta sesión: está dictado por `design/prototypes/HANDOFF.md` §11, que excluye explícitamente "pasarela de pagos real" de todo el alcance del prototipo. El revisor debe confirmarlo por grep (`stripe|pse|wompi|checkout\.` y variantes de URLs de cobro) sobre el diff completo antes de aprobar — última línea del `acceptance`.

## Decisiones ya tomadas en el documento de diseño (NO reabrir)

Documento de diseño aprobado por el usuario (base de este plan): `/Users/javergara/.claude/plans/ahora-has-un-nuevo-structured-pretzel.md`

- **Modelo** `models/sponsorship.py` (nuevo, vía skill `db-migrations`): `Sponsorship` — `id`, `user_id` (FK `users.id`), `pet_id` (FK `pets.id`), `monto_cop: int`, `periodicidad: str` (`String(20)`, valores válidos `"mensual"|"unico"`, documentado en comentario, validación real en el schema Pydantic — mismo criterio que `especie`/`tamano`/`estado` en el resto del repo, sin enum SQL), `activo: bool` (default `True`), `iniciado_en: datetime` (default `now(timezone.utc)`). Registrar en `models/__init__.py`.
- **Schemas** `schemas/sponsorship.py` (nuevo): `SponsorshipIn` (`user_id`, `pet_id`, `monto_cop` validado `>0`, `periodicidad` validado en `{"mensual","unico"}` vía `field_validator`, mismo patrón que `DescartarIn`); `SponsorshipOut` (id, resumen de pet, monto_cop, periodicidad, activo, iniciado_en, `novedad: str` estática si `activo`); `MascotaNecesitaApoyoOut` propio, **no hereda `PetOut`** (evita cargar `afinidad`/`shelter` que esta lista pública sin `user_id` no necesita): `id, nombre, fotos, historia, monto_recaudado_cop, porcentaje_cubierto`.
- **Router** `routers/sponsorships.py` (nuevo, mismo criterio que `routers/chat.py`):
  - `GET /api/pets/necesitan-apoyo` → mascotas `estado=="disponible"` filtradas por `es_dificil_de_ubicar` (reutilizada de `services/deck.py`, vía `PetOut` construido con `home=None`). Por cada una: `monto_recaudado_cop` = suma de `Sponsorship.monto_cop` con `activo=True` (`scalar_one() or 0`, porque `SUM` de cero filas es `NULL`). `META_MENSUAL_COP = 200_000` — constante de módulo con comentario explícito: *"Meta ilustrativa fija de demo, sin origen en ningún campo real de Pet/Shelter (HANDOFF §5.7)."* `porcentaje_cubierto = min(100, round(monto_recaudado_cop / META_MENSUAL_COP * 100))`. Lista vacía (200, no 404) si ninguna mascota califica.
  - `POST /api/sponsorships` → 404 español si `user_id`/`pet_id` no existen; Pydantic ya valida `monto_cop>0`/`periodicidad` (422 automático); crea con `activo=True`, 201.
  - `GET /api/users/{user_id}/sponsorships` → 404 si el usuario no existe; lista todos los apadrinamientos (activos e inactivos), con `novedad` de plantilla estática solo si `activo` (ej. *"¡{pet.nombre} está mejor gracias a tu ayuda! Este mes comió bien y tuvo su chequeo veterinario."*).
  - Registrar en `main.py` (mismo patrón que los 6 routers existentes).
- **Wiring de los stubs de 07/09** (mismo paso que el router, depende de que `Sponsorship` ya exista):
  - `routers/users.py::obtener_perfil` (y `registrar_usuario`, que sigue en `0` porque un usuario recién creado no tiene apadrinamientos — no requiere cambio de lógica, solo confirmar que sigue siendo correcto): `apadrinamientos` = count de `Sponsorship.activo` del `user_id`. Quitar el comentario "siempre 0" de `schemas/user.py::UserMetricsOut.apadrinamientos`.
  - `routers/shelters.py::obtener_refugio`: `apadrinamientos_recaudados_cop` = suma de `Sponsorship.monto_cop` (activos) vía join `Sponsorship`→`Pet` donde `Pet.shelter_id == shelter_id`. Quitar el comentario "siempre 0" del docstring y de `schemas/shelter.py::ShelterMetricsOut.apadrinamientos_recaudados_cop`. Actualizar el comentario espejo en `src/web/src/api/types.ts::ShelterMetrics`.
  - **No tocar** ninguna otra métrica de esas features (`matches_activos`, `visitas_agendadas`, `mascotas_publicadas`, `interesados_este_mes`, `adopciones_cerradas`).
- **Frontend**:
  - `api/types.ts`: `Sponsorship`, `SponsorshipIn`, `MascotaNecesitaApoyo` (espejo de los schemas nuevos).
  - `api/client.ts`: `listarNecesitanApoyo()`, `crearApadrinamiento(payload)`, `listarApadrinamientos(userId)`.
  - `screens/Apadrinar.tsx` (nuevo, ruta `/apadrinar`, patrón de formulario de `PublicarMascota.tsx` + fetch de `MiPerfil.tsx`): 3 niveles ($30.000 / $70.000 destacado / monto libre con input numérico), selector mensual/pago único, sección "Necesitan apoyo ahora" (`listarNecesitanApoyo()`, tarjetas con barra de progreso `bg-forest` sobre fondo claro y `%` cubierto), botón "Apadrinar" por mascota → `crearApadrinamiento({user_id: getActiveUserId(), pet_id, monto_cop, periodicidad})`.
  - `App.tsx`: ruta `/apadrinar` fuera de `RequiereHomeProfile` (igual que `/perfil`) + link de nav.
  - `MiPerfil.tsx`: nueva sección "Mis apadrinamientos" (`listarApadrinamientos(getActiveUserId())`) listando mascota/monto/periodicidad + novedad por apadrinamiento activo. La tarjeta de métrica `apadrinamientos` ya existente no necesita cambio de código — reflejará el valor real en cuanto el backend deje de stubear.
- **"Novedad mensual al padrino" — sin scheduler en el repo.** Igual que `09-shelter-panel` recortó las acciones de solicitud y `PublicarMascota.tsx` recortó el upload de fotos (ambos con justificación documentada), esta promesa se simplifica: una novedad de ejemplo **estática** por apadrinamiento activo, no generada por cron/tiempo real — decisión de alcance documentada, no un incumplimiento silencioso.
- **ADR**: esta feature NO requiere un ADR nuevo (a diferencia de `11-chat`) — no hay decisión de arquitectura/stack en juego, solo una tabla más sobre el mismo SQLite. Sí requiere actualizar `docs/architecture.md` §2 (quitar la nota "`Sponsorship` se añade cuando se retome la feature de backlog `12`" una vez implementada).

## `acceptance` de `12-sponsorship` en `feature_list.json` (7 líneas, ya escritas en esta sesión)

1. `GET /api/pets/necesitan-apoyo` devuelve solo mascotas que cumplen `services/deck.py::es_dificil_de_ubicar`, cada una con `monto_recaudado_cop` (suma de `Sponsorship.activo`) y `porcentaje_cubierto` contra una meta ilustrativa fija de demo, con tope de 100%.
2. `POST /api/sponsorships` crea el compromiso (`user_id`, `pet_id`, `monto_cop`, `periodicidad` `mensual|unico`) con 201, 404 en español si `user_id` o `pet_id` no existen, y rechaza `monto_cop <= 0`.
3. `GET /api/users/{user_id}/sponsorships` lista los apadrinamientos del usuario con resumen de la mascota y una novedad de ejemplo estática por apadrinamiento activo (sin scheduler real, decisión documentada de alcance).
4. `UserMetricsOut.apadrinamientos` y `ShelterMetricsOut.apadrinamientos_recaudados_cop` (features 07 y 09, ya aprobadas) dejan de estar fijos en 0 y reflejan datos reales de `Sponsorship`, sin reabrir el resto de esas features.
5. La pantalla `/apadrinar` muestra los tres niveles ($30.000, $70.000 destacado, monto libre), el selector mensual/pago único, la lista "Necesitan apoyo ahora" con barra de progreso y %, y el botón Apadrinar por mascota.
6. Mi perfil muestra una sección "Mis apadrinamientos" con los apadrinamientos activos del usuario y su novedad.
7. Cero integración con ninguna pasarela de pago real (sin Stripe/PSE/Wompi/URLs externas de cobro) — verificado explícitamente por el revisor antes de aprobar, consistente con HANDOFF §11.

## Secuenciación de pasos para el implementador (6 pasos, §7 del documento de diseño)

**Paso 0 — Líder (COMPLETADO en esta sesión):** `12-sponsorship` → `in_progress`, `acceptance` escrito en `feature_list.json`, este plan.

**Paso 1 — Backend: modelo + schemas — HECHO (implementador, 2026-08-03)**
- `src/api/adopta_api/models/sponsorship.py` (nuevo): clase `Sponsorship` tal cual el diseño (docstring explícito de "sin pasarela de pago real"). `models/__init__.py` la registra.
- `data/app.db` borrado y recreado con `python3 scripts/seed.py` (sin filas de ejemplo en `sponsorships`, no era requisito).
- `src/api/adopta_api/schemas/sponsorship.py` (nuevo): `SponsorshipIn` (validadores `monto_positivo`/`periodicidad_valida`), `SponsorshipPetResumen`, `SponsorshipOut` (`novedad: str | None`, se asigna en el router), `MascotaNecesitaApoyoOut` (no hereda `PetOut`).
- Verificado: import de `Sponsorship` sin error; `pytest tests/api -q` → 157/157 en verde (sin regresión); `ruff check`/`black --check` limpios; `bash init.sh` completo en verde (157 API + 55 frontend). Detalle completo en `changes.md` (entrada `12-sponsorship` paso 1).
- Pendiente: Paso 2 (router + wiring de stubs 07/09 + tests), Pasos 3-4 (frontend), Paso 5 (cierre del revisor). Ningún archivo de `routers/`, `main.py` ni frontend fue tocado en este paso.

**Paso 2 — Backend: router + wiring de stubs 07/09 + tests — HECHO (implementador, 2026-08-03)**
- `routers/sponsorships.py` (nuevo): `GET /api/pets/necesitan-apoyo`, `POST /api/sponsorships`, `GET /api/users/{user_id}/sponsorships`, tal cual el diseño. Registrado en `main.py` — **antes** que `pets.router` (no en el orden original al final): se descubrió por test real que `/api/pets/necesitan-apoyo` quedaba eclipsado por `/api/pets/{pet_id}` si se registraba después (422 al intentar parsear el slug como `pet_id` int); comentario explícito en `main.py` documentando el motivo.
- `routers/users.py::obtener_perfil` y `routers/shelters.py::obtener_refugio`: `apadrinamientos`/`apadrinamientos_recaudados_cop` reales (count/`SUM` de `Sponsorship`, `or 0` en la suma por `NULL` de SQL). Comentarios "siempre 0" quitados de `schemas/user.py`, `schemas/shelter.py`, docstring de `obtener_refugio`, y el espejo `src/web/src/api/types.ts::ShelterMetrics`. Ninguna otra métrica tocada.
- `tests/api/test_sponsorships.py` (nuevo, 19 tests). Gotcha resuelto: los `Pet` de prueba creados solo con `flush()` retenían `publicado_en` *aware* en memoria y `es_dificil_de_ubicar` (que resta contra un naive) tiraba `TypeError` — se corrigió haciendo `commit()`+`refresh()` en el helper del test (mismo patrón de gotcha ya documentado para `Match.creado_en` en `routers/shelters.py`).
- `tests/api/test_users.py::test_obtener_perfil_apadrinamientos_siempre_cero` renombrado a `test_obtener_perfil_apadrinamientos_es_cero_sin_sponsorships` (mismo `assert`, solo el nombre/docstring ya no reflejaban que ahora es un conteo real). Ningún otro test existente tocado.
- Verificado: `pytest tests/api -q` → **176/176 en verde** (157 previos + 19 nuevos). `ruff check`/`black --check` limpios. `bash init.sh` completo en verde: 176 API + 55 frontend. Detalle completo en `changes.md` (entrada `12-sponsorship` paso 2).
- Pendiente: Pasos 3-4 (frontend), Paso 5 (cierre del revisor). No se tocó ningún archivo de frontend en este paso.

**Paso 3 — Frontend: pantalla `Apadrinar.tsx` — HECHO (implementador, 2026-08-03)**
- `api/types.ts`: `SponsorshipPetResumen`, `Sponsorship`, `SponsorshipIn`, `MascotaNecesitaApoyo` (espejo exacto de `schemas/sponsorship.py`).
- `api/client.ts`: `listarNecesitanApoyo()`, `crearApadrinamiento(payload)`, `listarApadrinamientos(userId)` (usada recién en el paso 4, pero agregada aquí junto a las otras dos).
- `screens/Apadrinar.tsx` (nuevo): 3 niveles ($30.000 / $70.000 marcado "Recomendado" con `border-ochre` / monto libre con input numérico que solo aparece si está seleccionado), selector mensual (default)/pago único, sección "Necesitan apoyo ahora" (`listarNecesitanApoyo()`, skeleton, barra de progreso `bg-forest` + `%` + monto recaudado formateado es-CO, extracto de `historia` truncado a 140 chars), botón "Apadrinar" por mascota (deshabilitado si "monto libre" está vacío/`<=0`) que llama `crearApadrinamiento` y refresca la lista con un segundo fetch para reflejar el `porcentaje_cubierto` real del backend. Manejo de `ApiError` igual que `PublicarMascota.tsx`.
- `App.tsx`: ruta `/apadrinar` agregada fuera de `RequiereHomeProfile` (junto a `/perfil`) + `NavLink` "Apadrinar" en `Nav()`.
- `Apadrinar.test.tsx` (nuevo, 5 tests): render de niveles/periodicidad, validación de "monto libre" habilitando/deshabilitando el botón, submit con `$70.000`/`mensual` verificando el payload exacto de `crearApadrinamiento`, barra de progreso + `%` renderizados desde `listarNecesitanApoyo()` mockeado, mensaje de error de `ApiError`.
- Verificado: `npx vitest run` → **60/60 en verde** (55 previos + 5 nuevos); `npx tsc -b`, `npm run lint` (oxlint), `npx prettier --check .` limpios; `bash init.sh` completo en verde (176 API + 60 frontend). Detalle completo en `changes.md` (entrada `12-sponsorship` paso 3). No se tocó `MiPerfil.tsx` (paso 4) ni `src/api/`.
- Pendiente: Paso 4 (sección "Mis apadrinamientos" en `MiPerfil.tsx`), Paso 5 (cierre del revisor).

**Paso 4 — Frontend: sección "Mis apadrinamientos" en `MiPerfil.tsx` — HECHO (implementador, 2026-08-03)**
- `MiPerfil.tsx`: segundo `useEffect` (junto al que ya carga `obtenerPerfil`) que llama `listarApadrinamientos(getActiveUserId())`. Nueva sección "Mis apadrinamientos" después de "Sobre mí": skeleton mientras carga; estado vacío ("Todavía no apadrinas ninguna mascota." + `Link` a `/apadrinar`) si la lista viene vacía; si hay apadrinamientos, lista con foto/nombre/monto formateado es-CO (`formatoCop`) + periodicidad legible (`etiquetaPeriodicidad`), bloque de `novedad` (`bg-forest-tint`) solo si no es `null`, y etiqueta atenuada "Inactivo" junto al nombre si `activo === false`. La tarjeta de métrica "Apadrinamientos" (grid de 3, ya existente desde antes de esta feature) no requirió cambio de código — ya lee `metricas.apadrinamientos`, real desde el paso 2.
- `MiPerfil.test.tsx`: mock de `listarApadrinamientos` agregado; los 2 tests preexistentes ajustados a mockearlo con `[]` (si no, timeout por promesa sin resolver). 2 tests nuevos: lista vacía → copy + `Link` a `/apadrinar`; apadrinamiento activo con novedad → nombre, `"$70.000 · Mensual"`, texto exacto de la novedad, ausencia de la etiqueta "Inactivo".
- Verificado: `npx vitest run` → **62/62 en verde** (60 previos + 2 nuevos). `npx tsc -b`, `npm run lint` (oxlint), `npx prettier --check .` limpios. `bash init.sh` completo en verde de punta a punta: **176 tests de API + 62 de frontend**, lint/formato limpios — cierra los 4 pasos de implementación de `12-sponsorship`.
- **Verificación explícita de la restricción crítica (hecha por el implementador, no solo dejada al revisor)**: `command grep -rniE "stripe|pse|wompi|checkout\.|paypal|mercadopago|payu|epayco"` sobre todos los archivos backend+frontend nuevos/modificados de la feature (`models/sponsorship.py`, `routers/sponsorships.py`, `schemas/sponsorship.py`, `Apadrinar.tsx`, `Apadrinar.test.tsx`, `MiPerfil.tsx`, `MiPerfil.test.tsx`, `api/client.ts`, `api/types.ts`, `tests/api/test_sponsorships.py`) → **cero coincidencias reales**. Un `git diff` contra `main` sí arrojó un falso positivo de "pse" dentro de "Upsert" (docstring preexistente de `guardar_home_profile`, feature `08`, no relacionado) — descartado con `grep -o` y re-confirmado con `\bpse\b` (word boundary) sobre los archivos propios de la feature: cero matches. Tampoco hay `requests.`/`httpx.`/URLs externas en `models/sponsorship.py`, `routers/sponsorships.py` ni `schemas/sponsorship.py`.
- Detalle completo en `changes.md` (entrada `12-sponsorship` paso 4). Pendiente solo el Paso 5 (cierre del revisor, agente independiente). **No se cambió `status` en `feature_list.json`** (sigue `in_progress`) — eso es responsabilidad exclusiva del revisor.

## Cobertura de tests vs. `acceptance` de `12-sponsorship` (repasada por el implementador tras el Paso 4, para que el revisor solo tenga que confirmar)

| # | `acceptance` (resumen) | Test(s) que lo cubren |
|---|---|---|
| 1 | `GET /api/pets/necesitan-apoyo` filtra por `es_dificil_de_ubicar`, `monto_recaudado_cop`/`porcentaje_cubierto` con tope 100% | `tests/api/test_sponsorships.py::test_necesita_apoyo_filtra_por_es_dificil_de_ubicar`, `::test_necesita_apoyo_lista_vacia_sin_calificar_no_es_404`, `::test_necesita_apoyo_excluye_no_disponibles`, `::test_necesita_apoyo_porcentaje_cubierto_con_cero_apadrinamientos`, `::test_necesita_apoyo_porcentaje_cubierto_con_un_apadrinamiento`, `::test_necesita_apoyo_porcentaje_cubierto_tope_100`, `::test_necesita_apoyo_ignora_sponsorship_inactivo` |
| 2 | `POST /api/sponsorships` crea (201), 404 español si `user_id`/`pet_id` no existen, rechaza `monto_cop<=0` | `::test_crear_apadrinamiento_exitoso`, `::test_crear_apadrinamiento_usuario_inexistente_devuelve_404`, `::test_crear_apadrinamiento_mascota_inexistente_devuelve_404`, `::test_crear_apadrinamiento_monto_no_positivo_devuelve_422` (parametrizado `0`/`-1`/`-100`), `::test_crear_apadrinamiento_periodicidad_invalida_devuelve_422` |
| 3 | `GET /api/users/{id}/sponsorships` lista todos, novedad estática solo si `activo` | `::test_listar_apadrinamientos_usuario_incluye_activos_e_inactivos`, `::test_listar_apadrinamientos_novedad_solo_si_activo`, `::test_listar_apadrinamientos_usuario_inexistente_devuelve_404` |
| 4 | `UserMetricsOut.apadrinamientos`/`ShelterMetricsOut.apadrinamientos_recaudados_cop` reflejan datos reales, sin reabrir 07/09 | `::test_apadrinamiento_activo_refleja_en_metricas_usuario`, `::test_apadrinamiento_activo_refleja_en_metricas_refugio` (verifica también que el monto inactivo NO se suma); `tests/api/test_users.py::test_obtener_perfil_apadrinamientos_es_cero_sin_sponsorships` (caso base 0) |
| 5 | `/apadrinar`: 3 niveles, selector mensual/único, lista "Necesitan apoyo ahora" con progreso/%, botón Apadrinar | `src/web/src/screens/Apadrinar.test.tsx` (5 tests: niveles+periodicidad, validación de monto libre, submit con payload exacto, barra de progreso+%, error de `ApiError`) |
| 6 | Mi perfil: sección "Mis apadrinamientos" con activos y novedad | `src/web/src/screens/MiPerfil.test.tsx` (2 tests nuevos: estado vacío + link a `/apadrinar`, apadrinamiento activo con nombre/monto/periodicidad/novedad) |
| 7 | Cero integración con pasarela de pago real | Grep explícito corrido por el implementador (ver arriba) — cero coincidencias. El revisor debe repetirlo de forma independiente en el Paso 5 antes de aprobar (no basta con esta constancia). |

Los 7 `acceptance` de `12-sponsorship` tienen test real que los cubre. Ningún hueco detectado por el implementador al cierre del Paso 4.

**Paso 5 — Cierre del revisor (agente independiente, NO el líder ni el implementador)**
- `bash init.sh` completo en verde (backend + frontend), corrido en esta sesión (no heredado).
- **Grep explícito** de `stripe|pse|wompi|checkout\.` (y variantes razonables de URLs/SDKs de cobro externo) sobre el diff completo de la feature — debe dar cero resultados. Confirmar también que `POST /api/sponsorships` no hace ninguna llamada HTTP saliente (`requests`/`httpx` a dominios externos).
- Confirmar que los dos stubs de 07/09 ahora calculan valores reales (leer `routers/users.py::obtener_perfil` y `routers/shelters.py::obtener_refugio` línea por línea, no solo correr los tests) y que los comentarios "siempre 0" fueron efectivamente removidos de `schemas/user.py`, `schemas/shelter.py` y `src/web/src/api/types.ts`.
- Actualizar `docs/architecture.md` §2 (quitar la nota de "`Sponsorship` se añade cuando se retome la feature de backlog `12`").
- Recorrido manual en navegador real (`bash dev.sh`): abrir `/apadrinar`, confirmar que la lista "Necesitan apoyo ahora" muestra mascotas reales del seed con progreso; apadrinar una con el nivel $70.000; confirmar que el contador de métricas en `/perfil` (adoptante) y en `/refugio` (refugio de esa mascota) reflejan el cambio de inmediato; confirmar la sección "Mis apadrinamientos" en `/perfil` con la novedad estática.
- Confirmar programáticamente que ningún otro item de `feature_list.json` quedó `in_progress`.
- Resetear `data/app.db` con `python3 scripts/seed.py` después de la verificación manual.
- Solo entonces `status` → `done` en `feature_list.json`, con el mismo detalle de verificación que features anteriores documentado en `progress/current.md` (tabla acceptance↔test completa, como se hizo para `11-chat`).

## Verificación end-to-end esperada

- `bash init.sh` en verde tras cada paso (backend y frontend por separado durante implementación; completo al cierre).
- Recorrido manual en navegador real, detallado en el Paso 5.
- Resetear `data/app.db` con `python3 scripts/seed.py` después de cualquier verificación manual que haya mutado datos.

---

## Cierre de `11-chat` (revisor, sesión independiente, 2026-08-03) — resumen

**APROBADA.** `bash init.sh` en verde: 157 tests de API + 55 de frontend, lint/formato limpios. Las 7 líneas de `acceptance` verificadas una por una contra tests directos (historial idempotente + 404, mensajería WS persistida/difundida/aislada por hilo, cierre por ownership inválido, enlaces "Abrir conversación"/"Ver conversación", burbujas/chips solo lado adoptante, ADR `0004-chat-websockets-fastapi.md` leído completo contra la misma estructura de 0001-0003, `docs/architecture.md` §2/§6 confirmados actualizados). Detalle completo (incluida la tabla acceptance↔test y la revisión de código línea por línea) en el commit correspondiente y en la versión anterior de este archivo bajo control de versiones (`git log -p -- progress/current.md`).

## Cierre de `10-adoption-request-flow` (resumen)

**APROBADA** por el revisor (sesión independiente, 2026-08-03). `bash init.sh` en verde: 140 tests API + 45 frontend, lint/formato limpios. Matriz de transiciones backend (`services/solicitudes.py::validar_transicion`) vs. frontend (`SolicitudDetalle.tsx`) verificada línea por línea, sin discrepancias. `motivo_descarte` confirmado nunca expuesto al adoptante. Detalle completo en el commit `50c4482` y en la versión anterior de este archivo bajo control de versiones.

## Historial de cierres anteriores

Ver `progress/history.md` para el detalle completo de features `01`-`09` (todas `done`, aprobadas por revisor independiente). Resumen: MVP (`01`-`05`) aprobado 2026-07-31; `06-filters`/`07-adopter-profile` aprobadas en sesión posterior; `08-onboarding-cuestionario` aprobada 2026-08-03; `09-shelter-panel` aprobada 2026-08-03; `10-adoption-request-flow` aprobada 2026-08-03; `11-chat` aprobada 2026-08-03 (157 tests API + 55 frontend en verde). `12-sponsorship` planificada 2026-08-03, lista para que el implementador arranque por el Paso 1.
