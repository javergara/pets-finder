# Pines alineados con el runtime de Vercel (CPython 3.14, ADR 0007): toda
# dependencia compilada debe publicar wheel cp314 — verificado con
# `pip download <pkg>==<v> --python-version 314 --only-binary=:all:`.
# uvicorn va SIN [standard] a propósito: uvloop/httptools son compilados y solo
# sirven en dev local (el runtime serverless de Vercel no usa uvicorn).
fastapi==0.141.1
uvicorn==0.52.1
sqlalchemy==2.0.52
pydantic==2.13.4
requests==2.32.3
httpx==0.27.2
python-multipart==0.0.17
psycopg[binary]==3.3.4
